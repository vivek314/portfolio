name: Deploy

on:
  push:
    branches: [master, main]

jobs:
  deploy-backend:
    name: Deploy backend
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Railway
        run: echo "Configure Railway deploy action here"

  deploy-frontend:
    name: Deploy frontend
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Vercel
        run: echo "Configure Vercel deploy action here"
